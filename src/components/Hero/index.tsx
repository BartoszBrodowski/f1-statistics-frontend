import { QuoteIcon } from "@radix-ui/react-icons";

const Hero = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <h1>Hero</h1>
    </div>
  );
};

export default Hero;
